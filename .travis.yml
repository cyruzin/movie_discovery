language: node_js

# latest stable version
node_js:
  - "stable"

  cache:
  directories:
  - node_modules

  script:
  - npm install
  - npm test -- --coverage

# coveralls script
  after_script: 
  - npm run coveralls